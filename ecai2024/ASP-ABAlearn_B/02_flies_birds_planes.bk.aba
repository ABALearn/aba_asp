% Flies example from DK-95 + planes

bird(A) :- penguin(A).
penguin(A) :- superpenguin(A).
bird(A) :- A=woodstock.
bird(A) :- A=tweety.
penguin(A) :- A=pingu.
superpenguin(A) :- A=x_pingu.
plane(X) :- X=b777.
plane(X) :- X=a380.
plane(X) :- X=b747.
under_maint(X) :- X=b747.
