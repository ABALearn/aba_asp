% BK
quaker(a).
quaker(b).

republican(a).
republican(b).

%is_a(a).
%is_b(b).

% set_lopt(learning_mode(brave)).
% aba_asp('./examples/nixon_diamond.bk',[pacifist(a)],[pacifist(b)]).

dom(a).
dom(b).