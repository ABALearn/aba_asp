:- set_lopt(learning_mode(brave)).

:- set_lopt(folding_mode(lazy)).

:- set_lopt(folding_selection(any)).

%:- set_lopt(folding_space(all)).
:- set_lopt(folding_space(bk)).

:- set_lopt(asm_intro(relto)).

:- set_lopt(check_ic).

:- set_lopt(verbosity(info)).
:- set_lopt(clingo_time_limit(600)).
:- set_lopt(semantics(prf)).